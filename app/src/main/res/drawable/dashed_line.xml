<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="line">
    <stroke
        android:width="2dp"
        android:color="@color/netflix_light_gray"
        android:dashWidth="6dp"
        android:dashGap="6dp" />
</shape>
